---
import { Icon } from 'astro-icon/components';
const { href, icon } = Astro.props;
---

<a
    class="bg-orange-600 hover:bg-orange-500 transition-colors px-4 py-2 text-white text-xl rounded flex items-center gap-2"
    href={href}
>
    {icon ? <Icon name={icon} /> : null}
    <slot />
</a>
